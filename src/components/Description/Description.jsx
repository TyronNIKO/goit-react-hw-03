const Description = ({title, descr}) => {
    return (
        <>
            <h1 className="title">{title}</h1>
            <p className="descripton">{descr}</p>
        </>
    );
};

export default Description;
